---
title: 운영 알림 지표 구현 문서
slug: 대시보드-운영-알림-지표-구현-1차
type: deliverable
sdlc_phase: implementation
domain: 대시보드
subdomain: 운영 알림 지표
status: active
source_count: 2
sources:
- /home/lstguardleft/workspace/Project-Wiki-Manager/raw/20260528-150158-운영알림지표.md.md
- /home/lstguardleft/workspace/Project-Wiki-Manager/raw/20260528-204902-운영알림지표.md.md
updated: '2026-05-28'
---

# 대시보드 - 운영 알림 지표 - 구현

**문서 버전:** v2.0  
**도메인 / 서브도메인:** 대시보드 / 운영 알림 지표  
**SDLC 단계:** 구현  
**변경 요점 (vs 1차):** threshold query 처리, staleOrders 쿼리 추가, 응답 DTO 확장.

## 1. 구현 범위

`/api/dashboard/alerts`에 threshold·staleOrders를 도입. PENDING 주문 중 24시간 이상 경과한 건을 별도로 카운트한다.

## 2. 구현 항목 체크리스트

- [ ] OrderRepository에 `countByStatusAndOrderDateBefore(status, time)` 추가
- [ ] OrderRepository에 `countByStatus(OrderStatus.PENDING)` 추가
- [ ] ProductRepository에 `countByQuantityLessThan(int)` 추가
- [ ] AlertsResponse DTO 확장 (`staleOrders`, `threshold`)
- [ ] AlertsResponse DTO 정의 (2개 필드)
- [ ] DashboardService.getAlerts(int threshold) 작성
- [ ] DashboardController에 GET /api/dashboard/alerts 추가
- [ ] threshold 클램프 로직 (1~1000)
- [ ] Controller에 `@RequestParam(defaultValue="10")` 적용
- [ ] 단위/통합 테스트
- [ ] 단위 테스트 작성
- [ ] PENDING 상태 0건/100건 환경에서 카운트 정확성
- [ ] 재고 9개 상품 존재 시 카운트에 포함되는지

## 3. 적용 기술

- Spring `@RequestParam` 기본값/검증
- JPA derived query (`countByStatusAndOrderDateBefore`, `countByStatus`, `countByQuantityLessThan`)
- `LocalDateTime.now().minusHours(24)`

## 4. 단계별 작업 순서

1. Repository 메서드 추가  
2. 클램프 유틸  
3. Service  
4. Controller  
5. 테스트  

## 5. 위험 요소

| 위험 | 대응 |
|---|---|
| `orderDate` 인덱스 부재로 staleOrders 쿼리 느려짐 | DDL에 `idx_order_status_date` 추가 권장 |
| threshold 외부화로 부하 증가 가능 | 동일 쿼리 패턴이라 영향 적음, 모니터링 |

## 6. 검증 포인트

- threshold=0/5/100/-1/abc 각 케이스
- staleOrders가 정확히 24시간 경과분만 잡는지
- PENDING 상태 0건/100건 환경에서 카운트 정확성
- 재고 9개 상품 존재 시 카운트에 포함되는지

## 7. 연관 작업

| 연관 | 관계 |
|---|---|
| [[주문상태관리]] | OrderStatus enum |
| [[재고관리]] | quantity 필드 |

---

<!-- crossref:auto -->
## 8. 연관 도메인 / 서브도메인 / 관계

_이 표는 상호참조 Agent가 자동으로 유지합니다 — 직접 편집한 내용은 다음 적재 시 덮어쓰여집니다._

| 도메인 | 서브도메인 (페이지) | 관계 | 핵심 사유 | 함께 볼 때 |
|---|---|---|---|---|
| 대시보드 | 운영 알림 지표 [[대시보드-운영-알림-지표-설계-1차]] | 구체화 | 설계 문서가 구현 문서의 세부사항을 구체화함. | 구현 단계에서 설계 문서의 내용을 참고하여 알림 지표를 정확히 구현하기 위해 두 문서를 함께 검토해야 함. |
| 대시보드 | 운영 알림 지표 [[대시보드-운영-알림-지표-요구사항-1차]] | 연관 | 요구사항 문서가 구현의 기초를 제공함. | 요구사항을 이해하고 이를 바탕으로 구현을 진행하기 위해 두 문서를 함께 검토해야 함. |
| 대시보드 | 운영 알림 지표 [[대시보드-운영-알림-지표-설계-2차]] | 구현 | 설계 문서에서 정의한 기능을 구현함. | 설계 문서에서 정의한 기능이 어떻게 구현되었는지 확인할 때 두 문서를 함께 봐야 한다. |
| — | — [[대시보드-운영-알림-지표-요구사항-2차]] | 구현 | 요구사항 문서의 내용을 구현에 반영함. | 요구사항이 어떻게 구현되었는지 확인하기 위해 두 문서를 함께 참조해야 한다. |
<!-- /crossref:auto -->
