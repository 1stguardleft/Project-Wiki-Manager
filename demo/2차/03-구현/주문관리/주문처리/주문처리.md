# 주문 관리 - 주문 처리 - 구현 (2차)

**문서 버전:** v2.0 (2차)
**도메인 / 서브도메인:** 주문 관리 / 주문 처리
**SDLC 단계:** 구현
**변경 요점 (vs 1차):** employee_id/memo/금액 컬럼 추가, 재고 부족 거부, DELETE 시 복원.

## 1. 구현 범위

Order 엔티티 마이그레이션, OrderService.create/delete 리팩터링.

## 2. 구현 항목 체크리스트

- [ ] DDL: employee_id FK, memo, 금액 컬럼 4종
- [ ] OrderRequest DTO에 employeeId, memo, discountRate 추가
- [ ] EmployeeRepository.existsById 활용
- [ ] 등록 시 재고 부족 검증 → 409
- [ ] DELETE 시 status가 CANCELLED가 아니면 ProductService.restoreStock 호출
- [ ] 통합 테스트(동시 등록 시 재고 일관성)

## 3. 적용 기술

- JPA 마이그레이션
- 비관적 락(`@Lock(LockModeType.PESSIMISTIC_WRITE)`)
- @Transactional

## 4. 단계별 작업 순서

1. DDL 변경 → 2. 엔티티/DTO 동기화 → 3. 등록 검증 로직 → 4. DELETE 복원 → 5. 락 적용 → 6. 통합 테스트

## 5. 위험 요소

| 위험 | 대응 |
|---|---|
| 1차 클라이언트의 employeeId 누락 호출 | 400 errorCode 명시 + 마이그레이션 안내 |
| 비관적 락으로 처리량 저하 | 모니터링 후 낙관적 락 전환 검토 |

## 6. 검증 포인트

- 재고 부족 시 409 + 데이터 변경 없음
- DELETE 시 quantity 정확 복원
- 동시 등록 race 없음

## 7. 연관 작업

| 연관 | 관계 |
|---|---|
| [[재고연동]] | 검증·복원 호출 |
| [[금액계산]] | 신규 컬럼 |
| [[직원기본정보]] | employee FK |
